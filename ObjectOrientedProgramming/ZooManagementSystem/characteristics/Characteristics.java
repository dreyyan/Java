package OOP.ZooManagementSystem.characteristics;

public class Characteristics {
    public enum Color {
        RED, ORANGE, YELLOW, GREEN, BLUE, VIOLET,
        BROWN, BLACK, WHITE
    };

    public enum HealthStatus {
        DEAD, STARVING, NORMAL, HEALTHY
    };
}
